# Academic Portfolio

Static academic portfolio for Dr Shamendra Egodawela, deployed with GitLab Pages.

## Deployment

GitLab CI publishes the contents of `public/` whenever the default branch is updated.

The source website is maintained separately. Refresh the files in `public/`, commit, and push to publish an update.
